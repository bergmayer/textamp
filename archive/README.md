# Retired Plex implementation

`plex-2026-09-10.tar.gz` preserves the working source tree immediately before
Plex removal, including its intertwined application handlers, tests and docs.
It contains no build products or user configuration/credentials. It is not
compiled or shipped with Textamp. Shared media and navigation code continues
in the active tree; the archive preserves its original locations.
